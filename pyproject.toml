[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "redis-sample"
version = "0.1.0"
description = "Redis sample app"

[tool.setuptools]
packages = ["app", "benchmark", "charts"]
